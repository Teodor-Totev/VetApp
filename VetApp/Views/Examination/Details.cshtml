@model ExaminationViewModel

@{
	ViewData["Title"] = "Examination Details";
}

<div class="card border-secondary mb-3" style="max-width: 25rem; margin: 10px 0px">
	<h4 class="card-header">@Model.Status</h4>
	<div class="card-body">
		<dl class="row">
			<dt class="col-sm-5 card-title">Created On:</dt>
			<dd class="col-sm-5 card-text">@Model.CreatedOn.ToString("dd/MM/yyyy")</dd>

			<dt class="col-sm-5 card-title">Weight:</dt>
			<dd class="col-sm-5 card-text">@Model.Weight</dd>

			<dt class="col-sm-5 card-title">Reason:</dt>
			<dd class="col-sm-5 card-text">@Model.Reason</dd>

			<dt class="col-sm-5 card-title">Doctor:</dt>
			<dd class="col-sm-5 card-text">@Model.DoctorName</dd>

			<dt class="col-sm-5 card-title">Diagnosis:</dt>
			<dd class="col-sm-5 card-text">@Model.Diagnosis</dd>

			<dt class="col-sm-5 card-title">Surgery:</dt>
			<dd class="col-sm-5 card-text">@Model.Surgery</dd>

			<dt class="col-sm-5 card-title">Therapy:</dt>
			<dd class="col-sm-5 card-text">@Model.Therapy</dd>

			<dt class="col-sm-5 card-title">SpecificCondition:</dt>
			<dd class="col-sm-5 card-text">@Model.SpecificCondition</dd>

			<dt class="col-sm-5 card-title">CurrentCondition:</dt>
			<dd class="col-sm-5 card-text">@Model.CurrentCondition</dd>

			<dt class="col-sm-5 card-title">Exit:</dt>
			<dd class="col-sm-5 card-text">@Model.Exit</dd>

			<dt class="col-sm-5 card-title">MedicalHistory:</dt>
			<dd class="col-sm-5 card-text">@Model.MedicalHistory</dd>

			<dt class="col-sm-5 card-title">Research:</dt>
			<dd class="col-sm-5 card-text">@Model.Research</dd>
		</dl>
	</div>
</div>
